#we can use the mean() to show proportions of a logical vector
beads <- rep(c("red","blue"), times = c(2,3))
mean(beads =="blue")

#probability directly relates to proportion of TRUE events so this is a quick simple method of finding the proportion